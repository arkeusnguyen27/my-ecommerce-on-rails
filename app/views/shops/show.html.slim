.card-wrapper
  h1.title = @shop[:name]
  img.banner src= @shop[:banner_url]
  .description = @shop[:description]


.products-section
  h2 Products list
  .cards-wrapper
    - @products.each do |prod|
      = render "products/partials/product_card", { shop: @shop, product: prod }