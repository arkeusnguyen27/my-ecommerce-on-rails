<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand">
  <%= image_tag("home_images/logo.svg") %>
  </a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <%= link_to "Home", root_path, class:"nav-link" %>
      </li>
      <li class="nav-item">
        <%= link_to "All Shops", shops_path, class:"nav-link" %>
      </li>
      <li class="nav-item ">
      <% if current_user.present? %>
      <%= link_to "Dashboard", dashboard_index_path, class:"nav-link" %> 
      <% end %>
      </li>
      <li class="nav-item">
        <% if current_user.present? %>
          <%= link_to current_user.email, edit_user_registration_path, class:"nav-link" %>
        <% else %>
          <%= link_to "Sign in", new_user_session_path, class:"nav-link" %>
        <% end %>
      </li>

      <% if current_user.present? and current_user.buyer? %>
      <li class="nav-item ">
        <%= render 'layouts/navbars/nav_cart' %>
      </li>
      <% end %>

      <% if current_user.present? %>
        <li class="nav-item" id="sign-out-btn">
          <%= link_to "Sign Out", destroy_user_session_path, method: :delete, class:"nav-link" %>
        </li>
      <% else %>
        <li class="nav-item">
          <%= link_to "Sign up", new_user_registration_path, class:"nav-link" %>
        </li>
      <% end %>

    </ul>

  </div>
</nav>


